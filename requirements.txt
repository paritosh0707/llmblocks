# LLMBlocks - Core Requirements
# Production-ready AI framework with minimal dependencies

# Core dependencies
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
structlog>=23.2.0,<24.0.0
python-dotenv>=1.0.0,<2.0.0

# Async support
asyncio-mqtt>=0.16.0,<1.0.0  # For future MQTT support
aiofiles>=23.2.0,<24.0.0     # For async file operations

# LLM Provider dependencies
google-generativeai>=0.3.0,<1.0.0  # Gemini
openai>=1.0.0,<2.0.0                # OpenAI
anthropic>=0.7.0,<1.0.0             # Claude

# Optional: LangChain integration
langchain-core>=0.1.0,<1.0.0
langchain>=0.1.0,<1.0.0
langgraph>=0.0.20,<1.0.0

# Optional: Redis backend
redis>=5.0.0,<6.0.0

# Optional: Database backends
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0

# Development and testing (not needed in production)
# These are in pyproject.toml [project.optional-dependencies]
